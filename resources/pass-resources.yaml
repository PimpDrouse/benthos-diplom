input_resources:
  - label: "test_pass_input"
    kafka:
      addresses:
        - kafka:29092
      topics:
        - test-pass
      consumer_group: test_cons_grp

cache_resources:
  - label: pass_cache
    memory:
      compaction_interval: 20s
      default_ttl: 60s